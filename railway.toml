[build]
  builder = "NIXPACKS"
  buildCommand = "npm install --prefix server && npm install --prefix client && cd client && CI=false npm run build"

[deploy]
  startCommand = "node server/core/index.js"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10
